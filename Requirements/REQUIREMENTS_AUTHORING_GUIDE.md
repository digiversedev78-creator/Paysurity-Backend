# Requirements Authoring Guide (for requirements Agentic AI)

Mandatory:
- Single Source of Truth: CANONICAL.json
- Every requirement has acceptance criteria.
- MUST flow marked `done` requires E2E test file + traceability marker.

If implemented-but-undocumented is discovered:
- Add the missing requirement into CANONICAL at the correct semantic location.
- Mark status using evidence fields.
