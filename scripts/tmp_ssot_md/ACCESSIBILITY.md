# Accessibility (ADA + WCAG AA)

Default: enabled.

Automated CI checks:
- axe-core smoke scan on `/`

Manual before production:
- keyboard-only traversal on key MUST flows
- screen reader smoke test (NVDA/VoiceOver)
