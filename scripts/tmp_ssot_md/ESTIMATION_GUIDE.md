# Estimation guide (inputs needed)

Accuracy depends on:
- number of MUST flows (each implies E2E + UI + API work)
- integrations
- data model + state transitions
- security/compliance scope
- platform scope (web/mobile)
- deployment provider

Budget caps (optional):
- enable in PROJECT_PROFILE.budget
- record cost lines in AgentOutput/openai_usage.jsonl
